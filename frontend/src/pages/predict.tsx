import { Button} from "react-bootstrap";


const Predict = () => {
    return (
        <div className="page">
        <h1>Predict</h1>
        <Button variant="primary">Primary</Button>
        </div>
    );
};

export default Predict;